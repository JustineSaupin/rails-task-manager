<h1> <%= @task.title  %> :</h1>
<h3><%= @task.details  %></h3>

<%= link_to "tasks", tasks_path %>
<%= link_to 'edit', edit_task_path %>

<%= link_to "Suppress",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

